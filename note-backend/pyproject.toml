[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "nest-backend"
version = "0.1.0"
description = "衔泥 NestTray - Python backend sidecar"
requires-python = ">=3.10"
